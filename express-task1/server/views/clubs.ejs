<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Clubs</title>
  </head>
  <body>
    <ul>
      <% if (clubs) { %>
        <% clubs.forEach((club, index) => { %>
          <li style="display: flex; align-items: center;">
            <img src="<%= club.logo %>" width="50px" height="50px" alt=""> 
            <%= index + 1 %>. <%= club.title %>
          </li>
        <% }); %>
      <% } %>
    </ul>
  </body>
</html>
